#include <stdio.h>

int main() {
    int i,a[10],n;
    scanf("%d",&n);
    i=0;
    while(i<n)
    {
        scanf("%d",&a[i]);
        i++;
    }
    i=0;
    while(i<n)
    {
      printf("%d ",a[i]);
      i++;
    }
    return 0;
}
